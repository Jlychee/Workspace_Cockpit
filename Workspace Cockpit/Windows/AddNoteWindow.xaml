<Window x:Class="Workspace_Cockpit.Windows.AddNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Add note"
        Width="440"
        Height="420"
        MinWidth="440"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI">

    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="42"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource WindowBrush}"
            BorderBrush="{StaticResource LineBrush}"
            BorderThickness="1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- TITLEBAR -->
            <Border Grid.Row="0"
                    Background="#0D1117"
                    BorderBrush="{StaticResource Line2Brush}"
                    BorderThickness="0,0,0,1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="14,0,0,0">

                        <TextBlock Text="📝"
                                   FontSize="13"
                                   Margin="0,0,8,0" />

                        <TextBlock x:Name="WindowTitleTextBlock"
                                   Text="Add note"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="×"
                            Style="{StaticResource CloseTitleBarButton}"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"
                            FontSize="18"
                            Click="Close_Click" />
                </Grid>
            </Border>

            <!-- CONTENT AREA -->
            <Grid Grid.Row="1">

                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- SCROLLABLE CONTENT -->
                <ScrollViewer Grid.Row="0"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <Grid Margin="16,16,16,18">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="16" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- HEADER -->
                        <StackPanel Grid.Row="0">
                            <TextBlock x:Name="HeaderTitleTextBlock"
                                       Text="Новая заметка"
                                       FontSize="18"
                                       FontWeight="Bold" />

                            <TextBlock x:Name="HeaderDescriptionTextBlock"
                                       Text="Запиши мысль, напоминание или текущий контекст по workspace."
                                       Margin="0,4,0,0"
                                       FontSize="12"
                                       Foreground="{StaticResource MutedBrush}"
                                       TextWrapping="Wrap" />
                        </StackPanel>

                        <!-- TYPE -->
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="Тип заметки"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedBrush}"
                                       Margin="0,0,0,5" />

                            <ComboBox x:Name="NoteTypeComboBox"
                                      Height="34"
                                      Padding="8,4" />
                        </StackPanel>

                        <!-- TEXT -->
                        <StackPanel Grid.Row="4">
                            <TextBlock Text="Текст заметки"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource MutedBrush}"
                                       Margin="0,0,0,5" />

                            <TextBox x:Name="NoteTextBox"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalContentAlignment="Top"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled"
                                     MinHeight="120" />
                        </StackPanel>

                    </Grid>
                </ScrollViewer>

                <!-- FOOTER (FIXED) -->
                <Border Grid.Row="1"
                        Padding="12,10"
                        Background="#151B24"
                        BorderBrush="{StaticResource Line2Brush}"
                        BorderThickness="0,1,0,0">

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <Button Content="Cancel"
                                Margin="0,0,8,0"
                                Click="Cancel_Click" />

                        <Button x:Name="SaveButton"
                                Content="Save"
                                Style="{StaticResource PrimaryButton}"
                                Click="Save_Click" />

                    </StackPanel>
                </Border>

            </Grid>
        </Grid>
    </Border>
</Window>